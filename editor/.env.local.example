# Gemini API 配置
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-pro

# 代理配置（如果需要通过代理访问）
# HTTP_PROXY=http://proxy.server.com:8080
# HTTPS_PROXY=https://proxy.server.com:8443
# 或者使用 socks 代理
# HTTP_PROXY=socks5://127.0.0.1:1080
# HTTPS_PROXY=socks5://127.0.0.1:1080

# 代理认证（如果代理需要用户名密码）
# HTTP_PROXY=http://username:password@proxy.server.com:8080
# HTTPS_PROXY=https://username:password@proxy.server.com:8443

# API 配置
GEMINI_REQUEST_TIMEOUT=30000
GEMINI_MAX_RETRIES=3
GEMINI_RETRY_DELAY=1000

# 验证配置
ENABLE_HISTORICAL_VALIDATION=true
STRICT_RELATIONSHIP_CHECK=true
AUTO_BALANCE_SUGGESTION=true

# 开发环境配置
NODE_ENV=development
